global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:9090']  # Scrape metrics exported by the OpenTelemetry Collector
        labels:
          service: 'otel-collector'
